{
  "name": "bkataru-widgets",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub profile widget generator",
  "scripts": {
    "generate": "bun run scripts/generate_widgets.ts"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
